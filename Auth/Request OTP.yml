info:
  name: Request OTP
  type: http
  seq: 2

http:
  method: post
  url: "{{BaseURL}}/email/otp-request"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "ghavriel@dooeed.test"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
