info:
  name: Verify OTP
  type: http
  seq: 3

http:
  method: post
  url: "{{BaseURL}}/email/otp-verify"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "ghavriel@dooeed.test",
        "code": "775246"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
