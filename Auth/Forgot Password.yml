info:
  name: Forgot Password
  type: http
  seq: 5

http:
  method: post
  url: http://dooeed.test/api/v1/auth/forgot-password
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "oim@example.com"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
